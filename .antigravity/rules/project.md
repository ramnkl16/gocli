# Project rules (Antigravity)

Follow the repository root **`AGENTS.md`** for conventions, scope, and verification commands.

When `AGENTS.md` conflicts with a generic default, prefer **`AGENTS.md`**.
